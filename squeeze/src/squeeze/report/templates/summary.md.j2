# Squeeze 技術指標掃描 - 每日摘要
**日期:** {{ date }}

{% if count > 0 %}
## 總覽
今日共有 **{{ count }}** 檔標的符合 **買入** 篩選條件。

## 精選個股 (Top Picks)
以下個股顯示強勁動能或突破擠壓：

| 股票代碼 | 名稱 | 操作建議 | Squeeze 狀態 | 動能 (Momentum) | 能量 (Energy) |
| :--- | :--- | :--- | :--- | :--- | :--- |
{% for item in top_picks %}
| **{{ item.ticker }}** | {{ item.name }} | **{{ item.signal }}** | {{ '活躍' if item.squeeze_active else '非活躍' }} | {{ "%.4f"|format(item.momentum) if item.momentum else 'N/A' }} | {{ "%.4f"|format(item.energy) if item.energy else 'N/A' }} |
{% endfor %}

## 詳細結果
以下為完整掃描清單：

| 股票代碼 | 名稱 | 收盤價 | 操作建議 | 動能 (Momentum) | Squeeze 擠壓中 |
| :--- | :--- | :--- | :--- | :--- | :--- |
{% for item in results %}
| {{ item.ticker }} | {{ item.name }} | {{ item.close }} | {{ item.signal }} | {{ "%.4f"|format(item.momentum) if item.momentum else 'N/A' }} | {{ '是' if item.squeeze_active else '否' }} |
{% endfor %}
{% else %}
## 今日無符合買入條件的標的
目前的市場掃描中，尚無個股符合「強烈買入」或「買入」之動能條件。建議繼續觀察。
{% endif %}

---
*由 Squeeze Stock Screener 自動生成*
