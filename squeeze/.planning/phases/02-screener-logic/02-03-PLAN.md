---
phase: 02-screener-logic
plan: 03
type: tdd
wave: 3
depends_on: [02-02]
files_modified: [src/squeeze/engine/patterns.py, src/squeeze/cli.py, tests/unit/test_patterns.py, tests/integration/test_pattern_accuracy.py]
autonomous: true
requirements: [2.3]

must_haves:
  truths:
    - "User can run `squeeze scan --pattern whale` to find stocks matching the Whale Trading pattern"
    - "Pattern detection correctly identifies Squeeze ON on both Daily and Weekly timeframes"
    - "Alignment identifies momentum turning positive on both timeframes"
  artifacts:
    - path: "src/squeeze/engine/patterns.py"
      provides: "detect_whale_trading function"
    - path: "tests/integration/test_pattern_accuracy.py"
      provides: "Integration test for all patterns using real data"
  key_links:
    - from: "src/squeeze/engine/patterns.py"
      to: "pandas"
      via: "resampling for weekly timeframe alignment"
---

<objective>
Implement the "Whale Trading" multi-timeframe alignment logic.
Purpose: Identify institutional accumulation where energy is compressing on both long and short-term scales.
Output: Tested `detect_whale_trading` function, CLI support, and final accuracy tests.
</objective>

<feature>
  <name>Whale Trading Multi-Timeframe Alignment</name>
  <files>src/squeeze/engine/patterns.py, tests/unit/test_patterns.py</files>
  <behavior>
    - **Daily Check**: Verify Squeeze is ON for the Daily timeframe.
    - **Weekly Check**: Verify Squeeze is ON for the Weekly timeframe (resampled from Daily or fetched separately).
    - **Confirmation**: Momentum histogram must be increasing or positive on both timeframes.
  </behavior>
  <implementation>
    1. Define `detect_whale_trading(df_daily: pd.DataFrame) -> dict`.
    2. Resample `df_daily` to Weekly timeframe if necessary.
    3. Run indicators on both DataFrames.
    4. Check if both timeframes are in Squeeze.
    5. Compare momentum values.
    6. Return match status.
  </implementation>
</feature>

<execution_context>
@/Users/mingyenlin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mingyenlin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-screener-logic/02-RESEARCH.md
@src/squeeze/engine/patterns.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Whale Trading detection logic</name>
  <files>src/squeeze/engine/patterns.py, tests/unit/test_patterns.py</files>
  <behavior>
    - Test Case 1: Squeeze ON (Daily) + Squeeze ON (Weekly) + Momentum Positive -> Match.
    - Test Case 2: Only Daily Squeeze -> No Match.
    - Test Case 3: Both Squeezed but Momentum Negative -> No Match.
  </behavior>
  <action>
    - Add Whale Trading unit tests to `tests/unit/test_patterns.py`.
    - Implement `detect_whale_trading` in `src/squeeze/engine/patterns.py`.
    - Use resampled data to ensure alignment between timeframes.
  </action>
  <verify>
    <automated>pytest tests/unit/test_patterns.py -k whale</automated>
  </verify>
  <done>All Whale Trading unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: CLI integration for Whale</name>
  <files>src/squeeze/cli.py</files>
  <action>
    Modify `src/squeeze/cli.py`:
    - Add 'whale' choice to the `--pattern` option.
    - Wire `detect_whale_trading` to the scan command when 'whale' is selected.
    - Ensure output formatting clearly states Daily and Weekly status.
  </action>
  <verify>
    <automated>python -m src.squeeze scan --pattern whale --limit 5</automated>
  </verify>
  <done>User can specifically scan for Whale Trading patterns via CLI.</done>
</task>

<task type="auto">
  <name>Task 3: Pattern Accuracy Integration Tests</name>
  <files>tests/integration/test_pattern_accuracy.py</files>
  <action>
    Create `tests/integration/test_pattern_accuracy.py`.
    - Run the scanner with all patterns (squeeze, houyi, whale) against a controlled set of tickers.
    - Verify that no crashes occur across the full suite.
    - Check that metadata (energy level, momentum) is consistently returned.
  </action>
  <verify>
    <automated>pytest tests/integration/test_pattern_accuracy.py</automated>
  </verify>
  <done>Integration test confirms end-to-end scanner and pattern detection reliability.</done>
</task>

</tasks>

<verification>
Check `squeeze scan --pattern whale --limit 5` output for expected results.
</verification>

<success_criteria>
- Whale Trading detection logic passes TDD.
- CLI supports `--pattern whale`.
- Integration tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-screener-logic/02-03-SUMMARY.md`
</output>
