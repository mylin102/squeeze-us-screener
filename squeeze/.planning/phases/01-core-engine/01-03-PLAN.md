---
phase: 01-core-engine
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/squeeze/engine/indicators.py
  - src/squeeze/engine/__init__.py
  - src/squeeze/cli.py
  - tests/unit/test_indicators.py
autonomous: true
requirements: [1.4, 1.5]

must_haves:
  truths:
    - "Squeeze indicator matches reference values from the original `squeeze.py` implementation"
    - "Indicator calculation is vectorized using `pandas-ta` for high performance"
    - "Unit tests verify Bollinger Bands and Keltner Channel calculations"
  artifacts:
    - path: "src/squeeze/engine/indicators.py"
      provides: "Refactored Squeeze & Momentum indicator logic"
    - path: "tests/unit/test_indicators.py"
      provides: "Unit tests for technical indicators"
  key_links:
    - from: "src/squeeze/engine/indicators.py"
      to: "pandas_ta"
      via: "Function calls for BBANDS, KC, and ATR"
      pattern: "df\\.ta\\.(bbands|kc|atr)"
    - from: "src/squeeze/cli.py"
      to: "src/squeeze/data/downloader.py"
      via: "Function call to download data for analysis"
      pattern: "download_market_data"
    - from: "src/squeeze/cli.py"
      to: "src/squeeze/engine/indicators.py"
      via: "Function call to calculate squeeze indicators"
      pattern: "calculate_squeeze_indicators"
---

<objective>
Refactor the PowerSqueeze indicator engine to use `pandas-ta` for improved performance and reliability.

Purpose: Replacing hand-rolled technical analysis logic with a standard library like `pandas-ta` reduces bugs, improves execution speed, and simplifies the codebase.
Output: A modular, tested indicator engine that downstream screeners can consume.
</objective>

<execution_context>
@/Users/mingyenlin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mingyenlin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-engine/01-RESEARCH.md
@squeeze.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor Squeeze Indicator with pandas-ta</name>
  <files>src/squeeze/engine/indicators.py, src/squeeze/engine/__init__.py, tests/unit/test_indicators.py</files>
  <behavior>
    - Calculate 20-period Bollinger Bands (2.0 std) using `df.ta.bbands`.
    - Calculate 20-period Keltner Channels (1.5x ATR) using `df.ta.kc`.
    - Detect "Squeeze On" when BB is inside KC.
    - Calculate momentum using linear regression or MACD-like histogram (mirroring `squeeze.py` logic).
    - Assign energy levels (0-3) based on the compression ratio.
  </behavior>
  <action>
    - Create `src/squeeze/engine/__init__.py`.
    - Create `src/squeeze/engine/indicators.py`.
    - Implement `calculate_squeeze_indicators(df: pd.DataFrame) -> pd.DataFrame`.
    - Use `pandas-ta` extension for core calculations.
    - Port the energy level binning logic from `squeeze.py`.
    - Add unit tests in `tests/unit/test_indicators.py` using the OHLC fixture from `conftest.py`.
  </action>
  <verify>
    <automated>pytest tests/unit/test_indicators.py</automated>
  </verify>
  <done>Indicator engine produces accurate Squeeze signals using vectorized pandas-ta logic.</done>
</task>

<task type="auto">
  <name>Task 2: CLI Integration for Indicators</name>
  <files>src/squeeze/cli.py</files>
  <action>
    - Add a command `analyze --ticker 2330.TW` that downloads data and prints the latest Squeeze status.
    - Ensure it displays Energy Level and Momentum direction.
  </action>
  <verify>
    <automated>python -m squeeze analyze --ticker 2330.TW</automated>
  </verify>
  <done>CLI allows users to run the Squeeze analysis on a specific ticker.</done>
</task>

</tasks>

<verification>
Compare the output of the new engine against the output of the original `squeeze.py` for a known set of tickers to ensure parity.
</verification>

<success_criteria>
- Indicators calculate without error for both single and multiple timeframes.
- Squeeze detection logic is verified by unit tests with 100% coverage on edge cases (e.g., empty data).
- Execution time for 1000 rows of data is < 50ms.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-03-SUMMARY.md`
</output>
