---
phase: 03-reporting-visualization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/squeeze/report/visualizer.py, tests/integration/test_visualizer.py]
autonomous: true
requirements: [3.2]

must_haves:
  truths:
    - "Candlestick charts are generated as PNG images"
    - "Charts include Bollinger Bands and Keltner Channels as overlays"
    - "Charts include Squeeze dots at the bottom of the plot"
  artifacts:
    - path: "src/squeeze/report/visualizer.py"
      provides: "mplfinance chart generation"
  key_links:
    - from: "src/squeeze/report/visualizer.py"
      to: "mplfinance"
      via: "Chart plotting"
---

<objective>
Implement automated chart generation using `mplfinance` for technical analysis of top picks.

Purpose: Provide visual context for identified trading patterns.
Output: `visualizer.py` module for generating PNG charts.
</objective>

<execution_context>
@/Users/mingyenlin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mingyenlin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reporting-visualization/03-RESEARCH.md
@src/squeeze/engine/indicators.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Visualizer Logic</name>
  <files>src/squeeze/report/visualizer.py</files>
  <action>
    Develop `src/squeeze/report/visualizer.py`.
    - Function `plot_ticker(ticker_df, ticker_symbol, output_path)`: Uses `mplfinance.plot` with `make_addplot`.
    - Overlays: Bollinger Bands (dashed lines), Keltner Channels (solid lines).
    - Lower Panel: Squeeze Momentum Histogram and Squeeze Dots (red when on, green when off).
    - Volume: Standard volume bars at the bottom.
    - Title: Includes ticker symbol.
    - Handling Non-Trading Days: Ensure `show_nontrading=False` is set to avoid weekend gaps.
  </action>
  <verify>
    <automated>python3 -c "import mplfinance; from src.squeeze.report.visualizer import plot_ticker; print('Import successful')"</automated>
  </verify>
  <done>Chart generation module implemented using `mplfinance` with required overlays.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Visualizer Integration Tests</name>
  <files>tests/integration/test_visualizer.py</files>
  <behavior>
    - Mock data generation (pandas-ta data).
    - Call `plot_ticker` and verify file exists at `output_path`.
    - Verify file is a valid PNG and not 0 bytes.
  </behavior>
  <action>
    Implement integration tests in `tests/integration/test_visualizer.py`.
  </action>
  <verify>
    <automated>pytest tests/integration/test_visualizer.py</automated>
  </verify>
  <done>Integration tests confirm valid chart generation for dummy data.</done>
</task>

</tasks>

<verification>
Check visual output for correct color coding (red/green dots) and alignment of indicators.
</verification>

<success_criteria>
- `plot_ticker` produces valid PNG files.
- Charts include Bollinger Bands, Keltner Channels, and Squeeze indicator panel.
- All technical indicators align with price action.
</success_criteria>

<output>
After completion, create `.planning/phases/03-reporting-visualization/03-02-SUMMARY.md`
</output>
