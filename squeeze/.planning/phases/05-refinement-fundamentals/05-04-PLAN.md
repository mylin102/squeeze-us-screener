---
phase: 05-refinement-fundamentals
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified: [DOCS.md, README.md, pyproject.toml]
autonomous: true
requirements: [5.3]
must_haves:
  truths:
    - "User-facing documentation includes all new filtering and ranking options"
    - "README accurately describes the v1.0 state and features"
    - "Project dependencies are correctly listed for all new libraries"
  artifacts:
    - path: "DOCS.md"
      provides: "Detailed User Guide for the screener"
    - path: "README.md"
      provides: "Updated main documentation"
    - path: "pyproject.toml"
      provides: "Updated project dependencies"
---

<objective>
Complete the Phase 5 by documenting all new features and ensuring the project is ready for its v1.0 release.

Purpose: To provide clarity to users on how to use the advanced features and ensure consistent environment setup.
Output: Final documentation and dependency configuration.
</objective>

<execution_context>
@/Users/mingyenlin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mingyenlin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-refinement-fundamentals/05-03-SUMMARY.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final Documentation Update</name>
  <files>DOCS.md, README.md</files>
  <action>
    - Create `DOCS.md` with sections for:
      - Installation.
      - Core Squeeze Pattern Logic.
      - Fundamental Filtering options.
      - Value Score Ranking explanation.
      - Daily scanning setup (GitHub Actions).
    - Update `README.md` with:
      - Version 1.0 features list.
      - Quickstart guide for fundamental screening.
      - Link to `DOCS.md`.
  </action>
  <verify>
    <automated>test -f DOCS.md && grep "v1.0" README.md</automated>
  </verify>
  <done>Project is fully documented for end users.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize dependencies</name>
  <files>pyproject.toml</files>
  <action>
    - Ensure `tenacity`, `requests-cache`, `pyyaml` and `scipy` are added to `project.dependencies` in `pyproject.toml`.
    - Update the version to `1.0.0` in `pyproject.toml`.
    - Run `pip install .` to verify that dependencies install without conflict.
  </action>
  <verify>
    <automated>python3 -c "import tenacity; import scipy; import requests_cache; print('Success')"</automated>
  </verify>
  <done>Dependencies are finalized for v1.0.</done>
</task>

</tasks>

<verification>
Review `DOCS.md` and `README.md` for accuracy and formatting.
Ensure `pyproject.toml` version is set to `1.0.0`.
</verification>

<success_criteria>
- Comprehensive user guide created.
- Project version updated to v1.0.0.
- All new dependencies are listed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-refinement-fundamentals/05-04-SUMMARY.md`
</output>
