---
phase: 04-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/squeeze/report/notifier.py, src/squeeze/cli.py, pyproject.toml, tests/unit/test_notifier.py]
autonomous: true
requirements: [3.3, 4.3]

must_haves:
  truths:
    - "LINE Bot notifications can be sent using environment variables"
    - "CLI supports a --notify flag to trigger notifications after a scan"
  artifacts:
    - path: "src/squeeze/report/notifier.py"
      provides: "LineNotifier class"
  key_links:
    - from: "src/squeeze/cli.py"
      to: "src/squeeze/report/notifier.py"
      via: "LineNotifier.send_summary"
      pattern: "LineNotifier\\(\\).send_summary"
---

<objective>
Implement the LINE Bot notification system and enhance the CLI for automated runs.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement LINE Notifier</name>
  <files>src/squeeze/report/notifier.py</files>
  <action>
    - Create `src/squeeze/report/notifier.py`.
    - Implement `LineNotifier` class using `line-bot-sdk` v3.
    - Add `send_summary(message: str)` method.
    - Use `LINE_CHANNEL_ACCESS_TOKEN` and `LINE_USER_ID` from environment.
  </action>
  <verify>
    <automated>python3 -c "from squeeze.report.notifier import LineNotifier; print('Import successful')"</automated>
  </verify>
  <done>LineNotifier class implemented with MessagingApi integration.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance CLI for Automation</name>
  <files>src/squeeze/cli.py</files>
  <action>
    - Add `--notify` flag to `scan` command.
    - Integrate `LineNotifier` to send summary if `--notify` is set.
    - Ensure `--output-dir` defaults to `exports` if not specified.
  </action>
  <verify>
    <automated>python3 -m squeeze scan --help | grep -E "notify|output-dir"</automated>
  </verify>
  <done>CLI updated with --notify support and default output directory.</done>
</task>

<task type="auto">
  <name>Task 3: Update Dependencies</name>
  <files>pyproject.toml</files>
  <action>
    - Add `line-bot-sdk` to dependencies.
  </action>
  <verify>
    <automated>grep "line-bot-sdk" pyproject.toml</automated>
  </verify>
  <done>Project dependencies updated with LINE SDK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Unit Tests for Notifier</name>
  <files>tests/unit/test_notifier.py</files>
  <action>
    - Implement unit tests for `LineNotifier` using mocks to avoid real API calls.
  </action>
  <verify>
    <automated>python3 -m pytest tests/unit/test_notifier.py</automated>
  </verify>
  <done>Unit tests verify notification logic and environment variable handling.</done>
</task>

</tasks>
