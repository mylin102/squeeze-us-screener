---
phase: 05-refinement-fundamentals
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [src/squeeze/cli.py, tests/integration/test_fundamentals_integration.py]
autonomous: true
requirements: [5.1]
must_haves:
  truths:
    - "CLI 'scan' command supports --min-mkt-cap, --min-volume, and --min-score arguments"
    - "Fundamental filtering works in the CLI flow, excluding stocks that don't meet criteria"
    - "Integration tests confirm that filtering logic correctly excludes stocks in a real scan"
  artifacts:
    - path: "src/squeeze/cli.py"
      provides: "Updated CLI with fundamental filtering options"
    - path: "tests/integration/test_fundamentals_integration.py"
      provides: "End-to-end test for fundamental screening"
  key_links:
    - from: "src/squeeze/cli.py"
      to: "src/squeeze/engine/scanner.py"
      via: "passing filters to scanner.scan()"
---

<objective>
Expose the new fundamental filtering and ranking features to the CLI and validate through integration tests.

Purpose: To provide a complete user interface for fundamental-based screening and ensure reliability.
Output: Updated CLI and integration test suite.
</objective>

<execution_context>
@/Users/mingyenlin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mingyenlin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-refinement-fundamentals/05-02-SUMMARY.md
@src/squeeze/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI with fundamental filters</name>
  <files>src/squeeze/cli.py</files>
  <action>
    - Update the `scan` command in `src/squeeze/cli.py` (using `typer`) to include:
      - `--min-mkt-cap`: Minimum market capitalization (in billion TWD).
      - `--min-volume`: Minimum average daily volume.
      - `--min-score`: Minimum Value Score (0.0 - 1.0).
    - Ensure these filters are passed to `MarketScanner.scan()`.
    - Update the report generation to include the `value_score` in the output if available.
  </action>
  <verify>
    <automated>python3 -m src.squeeze.cli scan --help | grep "min-mkt-cap"</automated>
  </verify>
  <done>CLI now supports fundamental filtering options.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fundamental integration tests</name>
  <files>tests/integration/test_fundamentals_integration.py</files>
  <behavior>
    - Mock yfinance data for several tickers with different fundamental values.
    - Run the scanner with different filters and verify that only the expected tickers remain in the results.
    - Test the Value Score ranking in a real-world multi-ticker scenario.
  </behavior>
  <action>
    - Create `tests/integration/test_fundamentals_integration.py`.
    - Use `unittest.mock` to simulate `yf.download` and `yf.Tickers` data.
    - Verify that `MarketScanner` correctly applies filters and the ranker correctly assigns scores.
  </action>
  <verify>
    <automated>pytest tests/integration/test_fundamentals_integration.py</automated>
  </verify>
  <done>Integration tests confirm end-to-end fundamental filtering works correctly.</done>
</task>

</tasks>

<verification>
Run `python3 -m src.squeeze.cli scan --min-mkt-cap 100` and check output (with limited tickers).
Verify that the `value_score` is visible in the generated reports.
</verification>

<success_criteria>
- CLI supports new fundamental filters.
- Integration tests confirm the correctness of the fundamental-based screening flow.
</success_criteria>

<output>
After completion, create `.planning/phases/05-refinement-fundamentals/05-03-SUMMARY.md`
</output>
