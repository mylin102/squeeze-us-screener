---
phase: 05-refinement-fundamentals
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/squeeze/engine/ranker.py, src/squeeze/engine/scanner.py, src/squeeze/data/downloader.py]
autonomous: true
requirements: [5.1]
must_haves:
  truths:
    - "Value Score is correctly calculated based on percentile ranks of PE, PB, and Yield"
    - "MarketScanner correctly applies fundamental filters and ranking during scan"
    - "All yfinance calls in the project use the robust session"
  artifacts:
    - path: "src/squeeze/engine/ranker.py"
      provides: "calculate_value_score(df: pd.DataFrame) -> pd.DataFrame"
    - path: "src/squeeze/engine/scanner.py"
      provides: "Updated MarketScanner with fetch_fundamentals and filter_results"
    - path: "src/squeeze/data/downloader.py"
      provides: "Updated download_market_data using robust session"
  key_links:
    - from: "src/squeeze/engine/scanner.py"
      to: "src/squeeze/data/fundamentals.py"
      via: "import get_fundamentals"
    - from: "src/squeeze/engine/scanner.py"
      to: "src/squeeze/engine/ranker.py"
      via: "import calculate_value_score"
---

<objective>
Implement the Value Score ranking logic and integrate fundamentals into the MarketScanner.

Purpose: To allow users to filter for quality/value stocks and rank opportunities.
Output: Ranker module and updated MarketScanner.
</objective>

<execution_context>
@/Users/mingyenlin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mingyenlin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-refinement-fundamentals/05-RESEARCH.md
@.planning/phases/05-refinement-fundamentals/05-01-SUMMARY.md
@src/squeeze/engine/scanner.py
@src/squeeze/data/fundamentals.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Value Score ranking logic</name>
  <files>src/squeeze/engine/ranker.py, tests/unit/test_ranker.py</files>
  <behavior>
    - `calculate_value_score` takes a DataFrame of fundamental data.
    - Calculates percentile ranks for `trailingPE` (descending), `priceToBook` (descending), and `dividendYield` (ascending).
    - Returns the original DataFrame with a `value_score` column (0-1).
    - Handles NaNs by assigning the median rank or dropping them before ranking.
  </behavior>
  <action>
    - Create `src/squeeze/engine/ranker.py`.
    - Implement `calculate_value_score(df: pd.DataFrame) -> pd.DataFrame` using `pandas.Series.rank(pct=True)`.
    - Formula: `value_score = (pe_rank + pb_rank + yield_rank) / 3`.
  </action>
  <verify>
    <automated>pytest tests/unit/test_ranker.py</automated>
  </verify>
  <done>Ranking logic is correct and handles various input states.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate fundamentals into MarketScanner</name>
  <files>src/squeeze/engine/scanner.py, src/squeeze/data/downloader.py</files>
  <action>
    - Update `src/squeeze/data/downloader.py` to use `get_robust_session()` for `yf.download`.
    - Update `MarketScanner` class in `src/squeeze/engine/scanner.py`:
      - Add `fetch_fundamentals()` method that calls `get_fundamentals`.
      - Modify `scan()` to accept optional fundamental filters (min_mkt_cap, min_avg_volume).
      - Integrate `calculate_value_score` to add scores to scan results.
      - Apply filtering based on fundamental data before or after pattern detection.
  </action>
  <verify>
    <automated>python3 -c "from src.squeeze.engine.scanner import MarketScanner; scanner = MarketScanner(['2330.TW']); scanner.fetch_fundamentals(); print(scanner.fundamentals.columns)"</automated>
  </verify>
  <done>MarketScanner can now use fundamental data for filtering and ranking.</done>
</task>

</tasks>

<verification>
Verify `MarketScanner` results now include `value_score` and other fundamental fields.
Check that `download_market_data` uses the shared session correctly.
</verification>

<success_criteria>
- Ranking logic implemented and tested.
- `MarketScanner` integrated with fundamental data and filters.
- All data fetching uses robust session management.
</success_criteria>

<output>
After completion, create `.planning/phases/05-refinement-fundamentals/05-02-SUMMARY.md`
</output>
