---
phase: 05-refinement-fundamentals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/squeeze/core/session.py, src/squeeze/core/logger.py, src/squeeze/data/fundamentals.py]
autonomous: true
requirements: [5.1, 5.2]
must_haves:
  truths:
    - "A shared robust session with caching and retries exists for yfinance calls"
    - "Structured logging is configured for both console and file output"
    - "Fundamental data (market cap, volume, valuation metrics) can be fetched for a list of tickers"
  artifacts:
    - path: "src/squeeze/core/session.py"
      provides: "Shared requests session with tenacity retries and requests-cache"
    - path: "src/squeeze/core/logger.py"
      provides: "Configured logging setup"
    - path: "src/squeeze/data/fundamentals.py"
      provides: "get_fundamentals(tickers: List[str]) -> pd.DataFrame"
  key_links:
    - from: "src/squeeze/data/fundamentals.py"
      to: "src/squeeze/core/session.py"
      via: "importing get_robust_session"
---

<objective>
Establish the foundation for reliable data fetching and logging, and implement the fundamental data fetching module.

Purpose: To avoid rate limiting from Yahoo Finance and provide data for advanced filtering.
Output: Core reliability modules and a fundamentals data fetcher.
</objective>

<execution_context>
@/Users/mingyenlin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mingyenlin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-refinement-fundamentals/05-RESEARCH.md
@src/squeeze/data/downloader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement robust session and logging core</name>
  <files>src/squeeze/core/session.py, src/squeeze/core/logger.py</files>
  <action>
    - Create `src/squeeze/core/session.py` with a `get_robust_session()` function.
    - The session MUST use `requests-cache` (SQLite backend, 24h expiry) and a custom User-Agent.
    - Integrate `tenacity` retries (exponential backoff, jitter, max 5 attempts) specifically for `yfinance` 429 errors.
    - Create `src/squeeze/core/logger.py` to configure standard Python logging with a consistent format (e.g., `%(asctime)s - %(name)s - %(levelname)s - %(message)s`).
  </action>
  <verify>
    <automated>python3 -c "from src.squeeze.core.session import get_robust_session; session = get_robust_session(); print(session.headers['User-Agent'])"</automated>
  </verify>
  <done>Robust session and logging setup are ready for use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fundamental data fetching</name>
  <files>src/squeeze/data/fundamentals.py, tests/unit/test_fundamentals.py</files>
  <behavior>
    - `get_fundamentals(["2330.TW"])` should return a DataFrame with columns: ['ticker', 'marketCap', 'trailingPE', 'priceToBook', 'dividendYield', 'averageVolume'].
    - Handle missing data gracefully by returning NaN for missing fields.
    - Use `yf.Tickers` with the robust session created in Task 1.
  </behavior>
  <action>
    - Create `src/squeeze/data/fundamentals.py` with `get_fundamentals(tickers: List[str]) -> pd.DataFrame`.
    - Use the shared session from `src.squeeze.core.session`.
    - Fetch fundamental info using `yfinance` Ticker info keys.
  </action>
  <verify>
    <automated>pytest tests/unit/test_fundamentals.py</automated>
  </verify>
  <done>Fundamental data can be fetched in bulk for Taiwan tickers.</done>
</task>

</tasks>

<verification>
Check that `src/squeeze/core/session.py` correctly uses `requests_cache` and `tenacity`.
Verify `src/squeeze/data/fundamentals.py` returns the expected DataFrame structure.
</verification>

<success_criteria>
- Core reliability modules implemented.
- Fundamental data fetching module implemented and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/05-refinement-fundamentals/05-01-SUMMARY.md`
</output>
