---
phase: 01-core-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/squeeze/__init__.py
  - src/squeeze/__main__.py
  - src/squeeze/cli.py
  - src/squeeze/data/__init__.py
  - src/squeeze/data/tickers.py
  - tests/conftest.py
  - tests/integration/test_tickers.py
autonomous: true
requirements: [1.1, 1.2]

must_haves:
  truths:
    - "Can run `python -m squeeze` and see help output"
    - "Can fetch a list of 1000+ TWSE and TPEx tickers via CLI or direct call"
    - "Ticker discovery identifies both Listed (.TW) and OTC (.TWO) stocks correctly"
  artifacts:
    - path: "pyproject.toml"
      provides: "Dependency management (yfinance, pandas-ta, requests, typer, etc.)"
    - path: "src/squeeze/data/tickers.py"
      provides: "ISIN-based ticker discovery logic"
    - path: "tests/integration/test_tickers.py"
      provides: "Integration tests for ticker fetching"
  key_links:
    - from: "src/squeeze/data/tickers.py"
      to: "https://isin.twse.com.tw/isin/C_public.jsp"
      via: "requests GET with encoding='big5'"
      pattern: "requests\\.get"
---

<objective>
Initialize the project structure and implement the Taiwan ticker discovery logic.

Purpose: Establishing a solid foundation using the modern `src` layout and official ISIN ticker sources ensures long-term maintainability and data accuracy.
Output: A functional Python package structure with an automated ticker discovery engine.
</objective>

<execution_context>
@/Users/mingyenlin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mingyenlin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-engine/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Scaffolding & CLI Skeleton</name>
  <files>pyproject.toml, src/squeeze/__init__.py, src/squeeze/__main__.py, src/squeeze/cli.py, src/squeeze/data/__init__.py</files>
  <action>
    - Create `pyproject.toml` using the `src` layout. 
    - Include dependencies: `yfinance`, `pandas`, `pandas-ta`, `typer`, `requests`, `requests-cache`, `rich`, `mplfinance`.
    - Setup dev dependencies: `pytest`, `ruff`.
    - Implement a basic Typer CLI in `cli.py` with a placeholder `fetch-tickers` command.
    - Wire `__main__.py` to call the Typer app.
  </action>
  <verify>
    <automated>python -m squeeze --help</automated>
  </verify>
  <done>Project structure initialized and CLI entry point functional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ISIN-based Ticker Scraper</name>
  <files>src/squeeze/data/tickers.py, tests/integration/test_tickers.py</files>
  <behavior>
    - Should fetch from `https://isin.twse.com.tw/isin/C_public.jsp?strMode=2` (Listed) and `strMode=4` (OTC).
    - Should parse the HTML table using `pandas.read_html`.
    - Should filter for common stocks (4-digit numeric codes).
    - Should append `.TW` for Listed and `.TWO` for TPEx (OTC).
    - Should return a list of ticker symbols.
  </behavior>
  <action>
    - Implement `fetch_tickers` function in `src/squeeze/data/tickers.py`.
    - Use `requests` to fetch the pages (handle 'big5' encoding).
    - Add a command to `cli.py` that prints the number of discovered tickers.
  </action>
  <verify>
    <automated>pytest tests/integration/test_tickers.py</automated>
  </verify>
  <done>Ticker discovery returns a valid list of Taiwan market symbols with correct suffixes.</done>
</task>

<task type="auto">
  <name>Task 3: Shared Test Infrastructure</name>
  <files>tests/conftest.py</files>
  <action>
    - Create `tests/conftest.py` with common fixtures.
    - Add a fixture for a sample OHLC dataframe for testing indicators in later plans.
    - Setup any necessary mock data paths.
  </action>
  <verify>
    <automated>pytest --collect-only</automated>
  </verify>
  <done>Test configuration established for subsequent plans.</done>
</task>

</tasks>

<verification>
Ensure the project can be installed in editable mode (`pip install -e .`) and all tests pass.
</verification>

<success_criteria>
- Project structure follows `src` layout.
- `python -m squeeze` executes correctly.
- Ticker discovery fetches both TWSE and TPEx stocks with 95%+ accuracy compared to official lists.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-01-SUMMARY.md`
</output>
