---
phase: 01-core-engine
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/squeeze/data/downloader.py
  - tests/integration/test_download.py
autonomous: true
requirements: [1.3]

must_haves:
  truths:
    - "Can download daily OHLC data for a list of Taiwan tickers"
    - "Downloader uses a cached session to minimize redundant network requests"
    - "Downloader gracefully handles failed tickers without crashing the entire batch"
  artifacts:
    - path: "src/squeeze/data/downloader.py"
      provides: "Bulk data acquisition using yfinance"
    - path: "tests/integration/test_download.py"
      provides: "Validation for historical data fetching"
  key_links:
    - from: "src/squeeze/data/downloader.py"
      to: "yfinance.download"
      via: "Python function call with session and threads=True"
      pattern: "yf\\.download"
---

<objective>
Implement an optimized historical data downloader for Taiwan stocks.

Purpose: Bulk fetching of data for 1000+ stocks requires careful session management and error handling to avoid rate limits and ensure data completeness.
Output: A robust downloading module capable of fetching OHLC data efficiently.
</objective>

<execution_context>
@/Users/mingyenlin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mingyenlin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-engine/01-RESEARCH.md
@.planning/phases/01-core-engine/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Optimized yfinance Downloader</name>
  <files>src/squeeze/data/downloader.py, tests/integration/test_download.py</files>
  <behavior>
    - Use `requests-cache` to create a `CachedSession` with a real browser User-Agent.
    - Implement `download_market_data(tickers: list[str], period: str = '1y') -> pd.DataFrame`.
    - Use `yf.download` with `group_by='ticker'` and `threads=True` for bulk performance.
    - Handle empty dataframes by logging a warning instead of raising an error.
  </behavior>
  <action>
    - Create `src/squeeze/data/downloader.py`.
    - Implement session management as recommended in research.
    - Implement the bulk download logic.
    - Create a basic integration test that downloads data for a small sample of tickers (e.g., ["2330.TW", "2317.TW"]).
  </action>
  <verify>
    <automated>pytest tests/integration/test_download.py</automated>
  </verify>
  <done>Historical data can be fetched reliably for multiple Taiwan tickers.</done>
</task>

<task type="auto">
  <name>Task 2: CLI Integration for Downloader</name>
  <files>src/squeeze/cli.py</files>
  <action>
    - Add a command `download --ticker 2330.TW` to verify fetching via CLI.
    - Ensure it prints the shape of the returned dataframe.
  </action>
  <verify>
    <automated>python -m squeeze download --ticker 2330.TW</automated>
  </verify>
  <done>CLI provides basic access to data downloading functionality.</done>
</task>

</tasks>

<verification>
Ensure data is returned in a MultiIndex dataframe grouped by ticker, containing Open, High, Low, Close, and Volume.
</verification>

<success_criteria>
- Multi-ticker download completes successfully.
- Requests are cached locally (verify `yfinance.cache` creation).
- Missing tickers do not halt the process.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-02-SUMMARY.md`
</output>
