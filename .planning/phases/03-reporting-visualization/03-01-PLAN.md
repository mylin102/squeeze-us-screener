---
phase: 03-reporting-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/squeeze/report/__init__.py, src/squeeze/report/exporter.py, src/squeeze/report/templates/summary.md.j2, tests/unit/test_exporter.py]
autonomous: true
requirements: [3.1]

must_haves:
  truths:
    - "Scan results can be saved to CSV format"
    - "Scan results can be saved to JSON format"
    - "A human-readable Markdown summary is generated using Jinja2"
  artifacts:
    - path: "src/squeeze/report/exporter.py"
      provides: "CSV/JSON/MD export logic"
    - path: "src/squeeze/report/templates/summary.md.j2"
      provides: "Markdown report template"
  key_links:
    - from: "src/squeeze/report/exporter.py"
      to: "jinja2"
      via: "Template rendering"
---

<objective>
Implement the data export infrastructure for the Squeeze Stock Screener. This includes CSV, JSON, and human-readable Markdown reports.

Purpose: Allow users to persist and share scan results.
Output: `exporter.py` module and a Markdown template.
</objective>

<execution_context>
@/Users/mingyenlin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mingyenlin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reporting-visualization/03-RESEARCH.md
@src/squeeze/engine/scanner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Exporter Logic</name>
  <files>src/squeeze/report/__init__.py, src/squeeze/report/exporter.py</files>
  <action>
    Create `src/squeeze/report/exporter.py`.
    - Implement `ReportExporter` class.
    - Method `export(results: List[Dict], output_dir: Path)`: Orchestrates all exports.
    - Method `to_csv(results, path)`: Saves results to a flat CSV.
    - Method `to_json(results, path)`: Saves results to JSON with metadata (timestamp, pattern).
    - Method `to_markdown(results, path)`: Renders the Markdown summary using Jinja2.
    - Handle directory creation for `exports/YYYY-MM-DD/`.
  </action>
  <verify>
    <automated>python3 -c "from src.squeeze.report.exporter import ReportExporter; print('Import successful')"</automated>
  </verify>
  <done>ReportExporter class implemented with CSV, JSON, and MD export methods.</done>
</task>

<task type="auto">
  <name>Task 2: Create Markdown Template</name>
  <files>src/squeeze/report/templates/summary.md.j2</files>
  <action>
    Create a basic Jinja2 template for the Markdown summary.
    - Include a title with the scan date.
    - A table showing the matched tickers and their primary metrics (Momentum, Energy).
    - A section for "Top Picks" (if any).
  </action>
  <verify>
    <automated>ls src/squeeze/report/templates/summary.md.j2</automated>
  </verify>
  <done>Jinja2 template created for Markdown reports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit Tests for Exporter</name>
  <files>tests/unit/test_exporter.py</files>
  <behavior>
    - Mock scan results list.
    - Verify CSV file contains expected headers and data.
    - Verify JSON file is valid and contains metadata.
    - Verify Markdown file is rendered and contains ticker symbols.
  </behavior>
  <action>
    Implement unit tests in `tests/unit/test_exporter.py` using `pytest` and `tmp_path` fixture.
  </action>
  <verify>
    <automated>pytest tests/unit/test_exporter.py</automated>
  </verify>
  <done>Unit tests pass for all export formats.</done>
</task>

</tasks>

<verification>
Check that `exporter.py` correctly handles empty result lists and invalid output paths.
</verification>

<success_criteria>
- `ReportExporter` successfully generates three files (CSV, JSON, MD) for a given result set.
- All files are placed in the correct `YYYY-MM-DD` directory structure.
- Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-reporting-visualization/03-01-SUMMARY.md`
</output>
