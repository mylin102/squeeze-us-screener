---
phase: 03-reporting-visualization
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified: [src/squeeze/cli.py, tests/integration/test_cli_reporting.py]
autonomous: true
requirements: [3.3]

must_haves:
  truths:
    - "CLI supports --export flag for file output"
    - "CLI supports --plot flag for chart generation"
    - "CLI supports --top N flag for limiting chart generation"
  artifacts:
    - path: "src/squeeze/cli.py"
      provides: "Reporting CLI flags"
  key_links:
    - from: "src/squeeze/cli.py"
      to: "src/squeeze/report/exporter.py"
      via: "export_results"
    - from: "src/squeeze/cli.py"
      to: "src/squeeze/report/visualizer.py"
      via: "plot_results"
---

<objective>
Update the CLI to integrate reporting and visualization tools, enabling fully automated market analysis and export.

Purpose: Provide a unified interface for scanning, reporting, and visualizing.
Output: Enhanced `squeeze scan` command with export/plot support.
</objective>

<execution_context>
@/Users/mingyenlin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mingyenlin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reporting-visualization/03-RESEARCH.md
@src/squeeze/cli.py
@.planning/phases/03-reporting-visualization/03-01-SUMMARY.md
@.planning/phases/03-reporting-visualization/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI with Export/Plot Flags</name>
  <files>src/squeeze/cli.py</files>
  <action>
    Modify `src/squeeze/cli.py`:
    - Update `scan` command signature: Add `--export` (bool), `--plot` (bool), `--top` (int, default=10), `--output-dir` (Optional[Path]).
    - After scanning and filtering results:
      - If `--export` is True: Instantiate `ReportExporter` and call `export()`.
      - If `--plot` is True: Take the top N results (based on current sorting) and generate charts using `plot_ticker`.
    - Provide informative console messages using `rich`.
    - Handle edge cases (no matches found, output directory not writable).
  </action>
  <verify>
    <automated>squeeze scan --help | grep -E "export|plot|top"</automated>
  </verify>
  <done>CLI updated with functional reporting and plotting flags.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI Reporting Integration Tests</name>
  <files>tests/integration/test_cli_reporting.py</files>
  <behavior>
    - Run `squeeze scan --limit 5 --export --plot --top 2`.
    - Verify that the `exports/` directory is created.
    - Verify that CSV, JSON, and MD files exist in the daily subfolder.
    - Verify that exactly 2 PNG files exist in the `charts/` subfolder.
  </behavior>
  <action>
    Implement integration tests in `tests/integration/test_cli_reporting.py` using `typer.testing.CliRunner`.
  </action>
  <verify>
    <automated>pytest tests/integration/test_cli_reporting.py</automated>
  </verify>
  <done>End-to-end integration tests for CLI reporting pass.</done>
</task>

</tasks>

<verification>
Check for correct handling of `--top` (limit the number of charts generated) to prevent excessive runtime.
</verification>

<success_criteria>
- `squeeze scan --export --plot` successfully runs and produces files.
- Command-line flags are well-documented.
- System handles missing/empty results gracefully.
</success_criteria>

<output>
After completion, create `.planning/phases/03-reporting-visualization/03-03-SUMMARY.md`
</output>
