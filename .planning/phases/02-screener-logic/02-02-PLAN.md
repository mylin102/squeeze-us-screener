---
phase: 02-screener-logic
plan: 02
type: tdd
wave: 2
depends_on: [02-01]
files_modified: [src/squeeze/engine/patterns.py, src/squeeze/cli.py, tests/unit/test_patterns.py]
autonomous: true
requirements: [2.2]

must_haves:
  truths:
    - "User can run `squeeze scan --pattern houyi` to find stocks matching the Houyi pattern"
    - "Pattern detection identifies strong rallies followed by Fib retracements"
    - "Pattern detection correctly identifies the 'Shooting Star' candle within the Squeeze"
  artifacts:
    - path: "src/squeeze/engine/patterns.py"
      provides: "detect_houyi_shooting_sun function"
    - path: "tests/unit/test_patterns.py"
      provides: "Unit tests for Houyi pattern with synthetic data"
  key_links:
    - from: "src/squeeze/engine/patterns.py"
      to: "pandas_ta"
      via: "Fibonacci levels calculation"
---

<objective>
Implement the "Houyi Shooting the Sun" continuation pattern detection logic.
Purpose: Identify high-probability breakout opportunities from strong-uptrend consolidations.
Output: Tested `detect_houyi_shooting_sun` function and CLI integration.
</objective>

<feature>
  <name>Houyi Shooting the Sun Pattern Detection</name>
  <files>src/squeeze/engine/patterns.py, tests/unit/test_patterns.py</files>
  <behavior>
    - **Rally detection**: Identifies a preceding rally (e.g., >20% gain in 30 days).
    - **Retracement validation**: Price is within 0.5 - 0.618 Fibonacci retracement of the rally.
    - **Squeeze Check**: TTM Squeeze must be ON (BB inside KC).
    - **Shooting Star Candle**: Detects a candle where Upper Wick >= 2x Real Body.
  </behavior>
  <implementation>
    1. Define `detect_houyi_shooting_sun(df: pd.DataFrame) -> dict`.
    2. Calculate preceding peak and trough to find Fib levels.
    3. Check current price against Fib zone.
    4. Check `Squeeze_On` from indicators.
    5. Check candlestick wick-to-body ratio for the most recent 5 bars.
    6. Return match status and metadata.
  </implementation>
</feature>

<execution_context>
@/Users/mingyenlin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mingyenlin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-screener-logic/02-RESEARCH.md
@src/squeeze/engine/patterns.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Houyi pattern detection logic</name>
  <files>src/squeeze/engine/patterns.py, tests/unit/test_patterns.py</files>
  <behavior>
    - Test Case 1: Perfect Houyi setup (Rally -> Fib Retrace -> Squeeze -> Shooting Star) -> Match.
    - Test Case 2: No Rally -> No Match.
    - Test Case 3: Retrace too deep (> 0.7 Fib) -> No Match.
    - Test Case 4: No Squeeze -> No Match.
  </behavior>
  <action>
    - Create `tests/unit/test_patterns.py`.
    - Implement tests using synthetic OHLC DataFrames that represent each case.
    - Implement `detect_houyi_shooting_sun` in `src/squeeze/engine/patterns.py` to pass the tests.
    - Use `pandas_ta` or manual logic for Fib calculations as per research recommendation.
  </action>
  <verify>
    <automated>pytest tests/unit/test_patterns.py -k houyi</automated>
  </verify>
  <done>All Houyi detection unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: CLI integration for Houyi</name>
  <files>src/squeeze/cli.py</files>
  <action>
    Modify the `scan` command in `src/squeeze/cli.py`:
    - Add `--pattern` option (choices: 'squeeze', 'houyi').
    - Map choices to the corresponding function in `patterns.py`.
    - Ensure output formatting highlights the specific pattern requirements.
  </action>
  <verify>
    <automated>python -m src.squeeze scan --pattern houyi --limit 5</automated>
  </verify>
  <done>User can specifically scan for Houyi patterns via CLI.</done>
</task>

</tasks>

<verification>
Run `squeeze scan --pattern houyi`. It should execute without errors and filter results based on the new logic.
</verification>

<success_criteria>
- Houyi detection logic passes TDD.
- CLI supports `--pattern houyi`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-screener-logic/02-02-SUMMARY.md`
</output>
