---
phase: 02-screener-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/squeeze/engine/patterns.py, src/squeeze/engine/scanner.py, src/squeeze/cli.py, tests/integration/test_scanner.py]
autonomous: true
requirements: [2.1, 2.4]

must_haves:
  truths:
    - "User can run `squeeze scan` to find stocks currently in Squeeze"
    - "The scanner uses multi-processing for indicator analysis to ensure performance"
    - "The scanner handles full-market data fetching efficiently using batch downloads"
  artifacts:
    - path: "src/squeeze/engine/patterns.py"
      provides: "High-level pattern detection functions"
    - path: "src/squeeze/engine/scanner.py"
      provides: "MarketScanner class for full-market orchestration"
  key_links:
    - from: "src/squeeze/engine/scanner.py"
      to: "yfinance"
      via: "batch download with threads=True"
    - from: "src/squeeze/engine/scanner.py"
      to: "src/squeeze/engine/patterns.py"
      via: "ProcessPoolExecutor.map"
---

<objective>
Develop the core market scanning engine and implement basic Squeeze detection.
Purpose: Enable high-performance full-market screening for Taiwan stocks.
Output: Working `MarketScanner`, basic Squeeze detection logic, and a `scan` CLI command.
</objective>

<execution_context>
@/Users/mingyenlin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/mingyenlin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-screener-logic/02-RESEARCH.md
@src/squeeze/engine/indicators.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Basic Squeeze logic in patterns.py</name>
  <files>src/squeeze/engine/patterns.py</files>
  <action>
    Create `src/squeeze/engine/patterns.py`.
    Implement `detect_squeeze(df: pd.DataFrame) -> dict` function:
    - Calls `calculate_squeeze_indicators(df)` from `src.squeeze.engine.indicators`.
    - Returns a dictionary with `is_squeezed` (bool), `energy_level` (int), and `momentum` (float) from the latest bar.
  </action>
  <verify>
    <automated>python -c "import pandas as pd; from src.squeeze.engine.patterns import detect_squeeze; df = pd.DataFrame({'Open': [100]*30, 'High': [105]*30, 'Low': [95]*30, 'Close': [102]*30, 'Volume': [1000]*30}); print(detect_squeeze(df))"</automated>
  </verify>
  <done>`detect_squeeze` function implemented and returns correct state for the latest bar.</done>
</task>

<task type="auto">
  <name>Task 2: High-performance MarketScanner</name>
  <files>src/squeeze/engine/scanner.py</files>
  <action>
    Create `src/squeeze/engine/scanner.py`.
    Implement `MarketScanner` class:
    - `__init__(self, tickers: list)`
    - `fetch_data(self)`: Uses `yf.download(tickers, threads=True, group_by='ticker')` to bulk download data for the last 2 years (period="2y").
    - `scan(self, pattern_fn)`: Uses `ProcessPoolExecutor` to map `pattern_fn` across all tickers in the downloaded data. 
    - Handle edge cases: missing data, empty DataFrames, and rate limits.
    - Avoid re-downloading if data is already provided (allow injecting data for testing).
  </action>
  <verify>
    <automated>python -c "from src.squeeze.engine.scanner import MarketScanner; from src.squeeze.engine.patterns import detect_squeeze; scanner = MarketScanner(['2330.TW', '2317.TW']); scanner.fetch_data(); results = scanner.scan(detect_squeeze); print(results)"</automated>
  </verify>
  <done>`MarketScanner` successfully downloads bulk data and processes patterns in parallel.</done>
</task>

<task type="auto">
  <name>Task 3: Add scan command to CLI</name>
  <files>src/squeeze/cli.py</files>
  <action>
    Modify `src/squeeze/cli.py` to add a `scan` command:
    - Fetches tickers using `get_all_taiwan_tickers()` from `src.squeeze.data.tickers`.
    - Initializes `MarketScanner`.
    - Runs `scanner.fetch_data()` and `scanner.scan(detect_squeeze)`.
    - Displays results in a table or list (e.g., Ticker | Squeeze | Energy | Momentum).
    - Add `--limit` flag to test with a small subset of tickers.
  </action>
  <verify>
    <automated>python -m src.squeeze scan --limit 5</automated>
  </verify>
  <done>`squeeze scan` command works and shows basic Squeeze results for Taiwan stocks.</done>
</task>

<task type="auto">
  <name>Task 4: Scanner Integration Test</name>
  <files>tests/integration/test_scanner.py</files>
  <action>
    Create `tests/integration/test_scanner.py`.
    - Test `MarketScanner` with a small set of real tickers.
    - Verify that the hybrid thread/multiprocessing approach doesn't crash.
    - Assert that results contain expected keys (Squeeze_On, etc.).
  </action>
  <verify>
    <automated>pytest tests/integration/test_scanner.py</automated>
  </verify>
  <done>Integration test passes, confirming scanner reliability and basic pattern detection wiring.</done>
</task>

</tasks>

<verification>
Check `squeeze scan --limit 10` output. It should show a list of stocks with their squeeze status.
</verification>

<success_criteria>
- `src/squeeze/engine/scanner.py` implemented and functional.
- `squeeze scan` CLI command working.
- Integration test for scanner passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-screener-logic/02-01-SUMMARY.md`
</output>
