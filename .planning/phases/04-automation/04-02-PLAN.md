---
phase: 04-automation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [.github/workflows/daily_scan.yml, tests/integration/test_automation.py]
autonomous: true
requirements: [1.4, 4.1, 4.2]

must_haves:
  truths:
    - "GitHub Actions workflow is configured for daily execution"
    - "Scan results are automatically committed to the repository"
  artifacts:
    - path: ".github/workflows/daily_scan.yml"
      provides: "Daily scan workflow"
  key_links:
    - from: ".github/workflows/daily_scan.yml"
      to: "src/squeeze/cli.py"
      via: "GitHub Action step calling `squeeze scan`"
      pattern: "squeeze scan --export --plot --notify"
---

<objective>
Configure the daily execution pipeline and ensure end-to-end reliability.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions Workflow</name>
  <files>.github/workflows/daily_scan.yml</files>
  <action>
    - Create `.github/workflows/daily_scan.yml`.
    - Set schedule to `30 7 * * 1-5` (15:30 TST).
    - Include steps for checkout, python setup, dependency install.
    - Run `squeeze scan --export --plot --notify`.
    - Integrate `stefanzweifel/git-auto-commit-action` to push `exports/` folder.
  </action>
  <verify>
    <automated>test -f .github/workflows/daily_scan.yml</automated>
  </verify>
  <done>Workflow configured with scheduling and auto-commit logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration Tests for Automation Flow</name>
  <files>tests/integration/test_automation.py</files>
  <action>
    - Create `tests/integration/test_automation.py`.
    - Verify that `scan` command correctly triggers the notification logic (using mocks).
  </action>
  <verify>
    <automated>python3 -m pytest tests/integration/test_automation.py</automated>
  </verify>
  <done>Integration tests confirm end-to-end automation triggers and notifications.</done>
</task>

</tasks>
